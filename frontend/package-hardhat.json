{
  "name": "monsweeper-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for SecureMonsweeper with MockEntropy",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy-mock": "hardhat run deploy-mock-entropy.js --network monadTestnet",
    "deploy-local": "hardhat run deploy-mock-entropy.js --network hardhat",
    "test": "hardhat test",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "@pythnetwork/entropy-sdk-solidity": "^0.3.0"
  }
} 